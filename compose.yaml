services:
  db:
    image: alexandrias-db
    container_name: postgres:latest
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "8869:5432"
    volumes:
      - "./init/alexandrias.sql:/docker-entrypoint-initdb.d/1.sql"

